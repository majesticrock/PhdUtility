@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(
    Boost
    REQUIRED
    COMPONENTS
        serialization
        iostreams
)

include(
    "${CMAKE_CURRENT_LIST_DIR}/mrock_symbolic_operators-targets.cmake"
)