@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Include targets
if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/mrock_iEoM-targets.cmake")
    include("${CMAKE_CURRENT_LIST_DIR}/mrock_iEoM-targets.cmake")
endif()

# Ensure that the target is available
check_required_components(mrock_iEoM)